{
  "id": "marketing-persona-builder",
  "title": "Persona Builder",
  "description": "Persona typed from research. Vague personas. Alpha: auto-implemented typed agent — review before production.",
  "category": "marketing",
  "status": "alpha",
  "version": "0.1.0-alpha",
  "source": "agentskit-registry",
  "license": "MIT",
  "tags": [
    "marketing",
    "alpha",
    "structured-output"
  ],
  "packages": [
    "@agentskit/core",
    "@agentskit/runtime",
    "@agentskit/tools"
  ],
  "files": [
    "agent.ts",
    "README.md",
    "eval.ts"
  ],
  "requires": {
    "zod": "^3",
    "zod-to-json-schema": "^3"
  },
  "skill": {
    "name": "marketing-persona-builder",
    "description": "Persona typed from research. Vague personas. Alpha: auto-implemented typed agent — review before production.",
    "systemPrompt": "You are Persona Builder. Vague personas. Expected output: Persona typed from research.\n\nSummarize with insights grounded in input.\nNEVER invent facts absent from the input — use gaps and openQuestions.\nOutput is always a draft for human review.\n\n${UNTRUSTED_CONTENT_DIRECTIVE}\n\nCall submit_persona_builder exactly once with the structured result. Stop."
  },
  "flow": null,
  "a2a": {
    "id": "io.agentskit.registry.marketing-persona-builder",
    "name": "Persona Builder",
    "description": "Persona typed from research. Vague personas. Alpha: auto-implemented typed agent — review before production.",
    "version": "0.1.0-alpha",
    "homepage": "https://registry.agentskit.io",
    "skills": [
      {
        "name": "marketing-persona-builder",
        "description": "Persona typed from research. Vague personas. Alpha: auto-implemented typed agent — review before production.",
        "capabilities": {
          "streaming": true,
          "cancellation": true,
          "requiresApproval": false
        }
      }
    ]
  },
  "sources": [
    {
      "path": "agent.ts",
      "content": "import type { AdapterFactory, ChatMemory, Observer, ToolCall, ToolDefinition } from '@agentskit/core'\nimport { fenceUntrustedContent, UNTRUSTED_CONTENT_DIRECTIVE } from '@agentskit/core/security'\nimport { invokeStructured } from '@agentskit/runtime'\nimport { defineZodTool } from '@agentskit/tools'\nimport { z } from 'zod'\nimport { zodToJsonSchema } from 'zod-to-json-schema'\nimport type { JSONSchema7 } from 'json-schema'\n\n/**\n * Persona Builder — Persona typed from research\n * Pain: Vague personas\n * Status: alpha (auto-implemented; requires human review before validated).\n */\n\nexport interface AgentOutput { summary: string; insights: string[]; gaps: string[]; openQuestions: string[] }\n\nexport interface AgentResult extends AgentOutput {\n  requiresReview: boolean\n}\n\nexport interface MarketingPersonaBuilderConfig {\n  adapter: AdapterFactory\n  memory?: ChatMemory\n  observers?: Observer[]\n  onConfirm?: (toolCall: ToolCall) => boolean | Promise<boolean>\n  maxSteps?: number\n}\n\nconst Output = z.object({\n  summary: z.string(),\n  insights: z.array(z.string()),\n  gaps: z.array(z.string()).default([]),\n  openQuestions: z.array(z.string()).default([]),\n})\nconst toJson = (s: z.ZodTypeAny): JSONSchema7 => zodToJsonSchema(s) as JSONSchema7\n\n\n\nconst skill = {\n  name: 'marketing-persona-builder',\n  description: \"Persona Builder — typed output agent (draft spec).\",\n  systemPrompt: `You are Persona Builder. Vague personas. Expected output: Persona typed from research.\n\nSummarize with insights grounded in input.\nNEVER invent facts absent from the input — use gaps and openQuestions.\nOutput is always a draft for human review.\n\n${UNTRUSTED_CONTENT_DIRECTIVE}\n\nCall submit_persona_builder exactly once with the structured result. Stop.`,\n  tools: ['submit_persona_builder'],\n}\n\nexport function createMarketingPersonaBuilderAgent(config: MarketingPersonaBuilderConfig) {\n  const emit = (label: string, status: 'start' | 'ok' | 'skip' | 'error', detail?: string) => {\n    for (const o of config.observers ?? []) void o.on({ type: 'progress', label, status, detail })\n  }\n  const submit = (): ToolDefinition =>\n    defineZodTool({\n      name: 'submit_persona_builder',\n      description: 'Submit the typed result. Call exactly once.',\n      schema: Output,\n      toJsonSchema: toJson,\n      async execute() { return 'recorded' },\n    }) as ToolDefinition\n\n  async function run(input: string): Promise<AgentResult> {\n    if (!input?.trim()) throw new Error('marketing-persona-builder requires non-empty input')\n    emit('run', 'start')\n    const result = await invokeStructured({\n      adapter: config.adapter,\n      tool: submit(),\n      task: `INPUT:\\n${fenceUntrustedContent(input)}`,\n      parse: (a) => Output.parse(a),\n      skill,\n      memory: config.memory,\n      observers: config.observers,\n      onConfirm: config.onConfirm,\n      maxSteps: config.maxSteps ?? 4,\n    })\n    emit('run', 'ok')\n    return { ...result, requiresReview: true }\n  }\n\n  return {\n    name: 'marketing-persona-builder',\n    run,\n    asHandle() {\n      return { name: 'marketing-persona-builder', run: async (task: string) => JSON.stringify(await run(task)) }\n    },\n  }\n}\n"
    },
    {
      "path": "README.md",
      "content": "# Persona Builder\n\n> **Status: alpha** — installable via `npx agentskit add marketing-persona-builder` for experimentation. Not yet `validated`.\n\n## Pain\n\nVague personas\n\n## Output\n\nPersona typed from research\n\n## Usage\n\n```ts\nimport { createMarketingPersonaBuilderAgent } from './agents/marketing-persona-builder/agent'\nconst result = await createMarketingPersonaBuilderAgent({ adapter }).run(input)\n```\n\n## Gates\n\n- typed-output\n- never-invent\n- always-draft\n\n## Promote to validated\n\nHuman review + expand `eval.ts` + set `status: validated` in meta.json.\n"
    },
    {
      "path": "eval.ts",
      "content": "import type { EvalSuite } from '@agentskit/eval'\n\nexport const suite: EvalSuite = {\n  name: 'marketing-persona-builder',\n  cases: [\n    {\n      input: 'Sample input for Persona Builder: Vague personas. Provide structured output.',\n      expected: (r: string) => r.length > 10,\n    },\n    {\n      input: 'Minimal input — no details provided. List gaps instead of inventing.',\n      expected: (r: string) => /gap|openQuestion|confirm|missing/i.test(r) || r.length > 5,\n    },\n  ],\n}\n"
    }
  ],
  "installable": true
}

{
  "id": "product-experiment-designer",
  "title": "Experiment Designer",
  "description": "Design typed. Experiments poorly designed. Alpha: auto-implemented typed agent — review before production.",
  "category": "product",
  "status": "alpha",
  "version": "0.1.0-alpha",
  "source": "agentskit-registry",
  "license": "MIT",
  "tags": [
    "product",
    "alpha",
    "structured-output"
  ],
  "packages": [
    "@agentskit/core",
    "@agentskit/runtime",
    "@agentskit/tools"
  ],
  "files": [
    "agent.ts",
    "README.md",
    "eval.ts"
  ],
  "requires": {
    "zod": "^3",
    "zod-to-json-schema": "^3"
  },
  "skill": {
    "name": "product-experiment-designer",
    "description": "Design typed. Experiments poorly designed. Alpha: auto-implemented typed agent — review before production.",
    "systemPrompt": "You are Experiment Designer. Experiments poorly designed. Expected output: Design typed.\n\nSummarize with insights grounded in input.\nNEVER invent facts absent from the input — use gaps and openQuestions.\nOutput is always a draft for human review.\n\n${UNTRUSTED_CONTENT_DIRECTIVE}\n\nCall submit_experiment_designer exactly once with the structured result. Stop."
  },
  "flow": null,
  "a2a": {
    "id": "io.agentskit.registry.product-experiment-designer",
    "name": "Experiment Designer",
    "description": "Design typed. Experiments poorly designed. Alpha: auto-implemented typed agent — review before production.",
    "version": "0.1.0-alpha",
    "homepage": "https://registry.agentskit.io",
    "skills": [
      {
        "name": "product-experiment-designer",
        "description": "Design typed. Experiments poorly designed. Alpha: auto-implemented typed agent — review before production.",
        "capabilities": {
          "streaming": true,
          "cancellation": true,
          "requiresApproval": false
        }
      }
    ]
  },
  "sources": [
    {
      "path": "agent.ts",
      "content": "import type { AdapterFactory, ChatMemory, Observer, ToolCall, ToolDefinition } from '@agentskit/core'\nimport { fenceUntrustedContent, UNTRUSTED_CONTENT_DIRECTIVE } from '@agentskit/core/security'\nimport { invokeStructured } from '@agentskit/runtime'\nimport { defineZodTool } from '@agentskit/tools'\nimport { z } from 'zod'\nimport { zodToJsonSchema } from 'zod-to-json-schema'\nimport type { JSONSchema7 } from 'json-schema'\n\n/**\n * Experiment Designer — Design typed\n * Pain: Experiments poorly designed\n * Status: alpha (auto-implemented; requires human review before validated).\n */\n\nexport interface AgentOutput { summary: string; insights: string[]; gaps: string[]; openQuestions: string[] }\n\nexport interface AgentResult extends AgentOutput {\n  requiresReview: boolean\n}\n\nexport interface ProductExperimentDesignerConfig {\n  adapter: AdapterFactory\n  memory?: ChatMemory\n  observers?: Observer[]\n  onConfirm?: (toolCall: ToolCall) => boolean | Promise<boolean>\n  maxSteps?: number\n}\n\nconst Output = z.object({\n  summary: z.string(),\n  insights: z.array(z.string()),\n  gaps: z.array(z.string()).default([]),\n  openQuestions: z.array(z.string()).default([]),\n})\nconst toJson = (s: z.ZodTypeAny): JSONSchema7 => zodToJsonSchema(s) as JSONSchema7\n\n\n\nconst skill = {\n  name: 'product-experiment-designer',\n  description: \"Experiment Designer — typed output agent (draft spec).\",\n  systemPrompt: `You are Experiment Designer. Experiments poorly designed. Expected output: Design typed.\n\nSummarize with insights grounded in input.\nNEVER invent facts absent from the input — use gaps and openQuestions.\nOutput is always a draft for human review.\n\n${UNTRUSTED_CONTENT_DIRECTIVE}\n\nCall submit_experiment_designer exactly once with the structured result. Stop.`,\n  tools: ['submit_experiment_designer'],\n}\n\nexport function createProductExperimentDesignerAgent(config: ProductExperimentDesignerConfig) {\n  const emit = (label: string, status: 'start' | 'ok' | 'skip' | 'error', detail?: string) => {\n    for (const o of config.observers ?? []) void o.on({ type: 'progress', label, status, detail })\n  }\n  const submit = (): ToolDefinition =>\n    defineZodTool({\n      name: 'submit_experiment_designer',\n      description: 'Submit the typed result. Call exactly once.',\n      schema: Output,\n      toJsonSchema: toJson,\n      async execute() { return 'recorded' },\n    }) as ToolDefinition\n\n  async function run(input: string): Promise<AgentResult> {\n    if (!input?.trim()) throw new Error('product-experiment-designer requires non-empty input')\n    emit('run', 'start')\n    const result = await invokeStructured({\n      adapter: config.adapter,\n      tool: submit(),\n      task: `INPUT:\\n${fenceUntrustedContent(input)}`,\n      parse: (a) => Output.parse(a),\n      skill,\n      memory: config.memory,\n      observers: config.observers,\n      onConfirm: config.onConfirm,\n      maxSteps: config.maxSteps ?? 4,\n    })\n    emit('run', 'ok')\n    return { ...result, requiresReview: true }\n  }\n\n  return {\n    name: 'product-experiment-designer',\n    run,\n    asHandle() {\n      return { name: 'product-experiment-designer', run: async (task: string) => JSON.stringify(await run(task)) }\n    },\n  }\n}\n"
    },
    {
      "path": "README.md",
      "content": "# Experiment Designer\n\n> **Status: alpha** — installable via `npx agentskit add product-experiment-designer` for experimentation. Not yet `validated`.\n\n## Pain\n\nExperiments poorly designed\n\n## Output\n\nDesign typed\n\n## Usage\n\n```ts\nimport { createProductExperimentDesignerAgent } from './agents/product-experiment-designer/agent'\nconst result = await createProductExperimentDesignerAgent({ adapter }).run(input)\n```\n\n## Gates\n\n- typed-output\n- never-invent\n- always-draft\n\n## Promote to validated\n\nHuman review + expand `eval.ts` + set `status: validated` in meta.json.\n"
    },
    {
      "path": "eval.ts",
      "content": "import type { EvalSuite } from '@agentskit/eval'\n\nexport const suite: EvalSuite = {\n  name: 'product-experiment-designer',\n  cases: [\n    {\n      input: 'Sample input for Experiment Designer: Experiments poorly designed. Provide structured output.',\n      expected: (r: string) => r.length > 10,\n    },\n    {\n      input: 'Minimal input — no details provided. List gaps instead of inventing.',\n      expected: (r: string) => /gap|openQuestion|confirm|missing/i.test(r) || r.length > 5,\n    },\n  ],\n}\n"
    }
  ],
  "installable": true
}
